-- Run AFTER you create an admin user in Supabase → Authentication → Users
-- Replace the UUID with your user's id from the Users table.

-- Example:
-- insert into public.admin_users (user_id)
-- values ('aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee')
-- on conflict (user_id) do nothing;
